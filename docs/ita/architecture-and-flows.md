explains the high‑level architecture: frontend (HTML/CSS/JS + Bootstrap), backend (Node.js + Express + REST + Swagger), MongoDB integration.
it also references the main flow diagrams (saved in docs/diagrams/), such as the order + home delivery flow and the registration/login flow.
